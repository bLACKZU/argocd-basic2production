apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: app1-git-files
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error

  generators:
    - git:
        repoURL: https://github.com/bLACKZU/argocd-basic2production.git
        revision: main
        files:
          - path: ApplicationSets/05-git-file-generator/clusters.yaml

  template:
    metadata:
      name: "{{.name}}"
    spec:
      project: default

      source:
        repoURL: https://github.com/bLACKZU/argocd-basic2production.git
        targetRevision: main
        path: "ApplicationSets/05-git-file-generator/{{.region}}/manifests"

      destination:
        name: "{{.cluster}}"
        namespace: "{{.namespace}}"

      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true